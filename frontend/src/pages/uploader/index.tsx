import React from 'react'
import { RouteComponentProps } from '@reach/router'

import { Layout } from '../../layout'

export const Uploader = (props:RouteComponentProps) => {

  return (
    <Layout>
      Hello from uploader :D
    </Layout>
  )
}
